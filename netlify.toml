[build]
  command = "go build -o bin/jurnalinal main.go"
  publish = "static"

[functions]
  directory = "functions"

# Note: To run a full Gin server on Netlify, you typically wrap it 
# with github.com/awslabs/aws-lambda-go-api-proxy/gin in a sub-package.
# Recommendation: For Go Gin apps with HTML templates, Render or Railway 
# is highly recommended over Netlify as they support full binaries.
